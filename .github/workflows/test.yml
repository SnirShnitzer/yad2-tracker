name: Test Yad2 Tracker

on:
  pull_request:
    branches: [ main, master ]
  push:
    branches: [ main, master ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: 'tracker-service/package-lock.json'
        
    - name: Install dependencies
      run: |
        cd tracker-service
        npm ci
      
    - name: Build TypeScript
      run: |
        cd tracker-service
        npm run build
      
    - name: Test build (no actual scraping)
      run: |
        echo "✅ TypeScript compilation successful"
        echo "✅ All dependencies installed"
        echo "✅ Build artifacts created"
        ls -la tracker-service/dist/
        
    - name: Validate configuration
      run: |
        echo "🔍 Checking configuration files..."
        if [ -f "tracker-service/src/config.ts" ]; then
          echo "✅ Config file exists"
        else
          echo "❌ Config file missing"
          exit 1
        fi
        
        if [ -f "tracker-service/src/types.ts" ]; then
          echo "✅ Types file exists"
        else
          echo "❌ Types file missing"
          exit 1
        fi
